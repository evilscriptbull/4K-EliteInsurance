# Implementation Backlog

Derived from the roadmap in the handoff doc. Split MVP / Phase 2+ / Later, per the doc's MVP boundary.

## MVP (ship the acquisition foundation)

- [x] Repo scaffold (Next.js + TS + Tailwind), buildable/lint-clean
- [x] Canonical `Lead` and `ConversationState` schemas
- [x] Compliance guardrails skeleton + 14-state disclaimer structure
- [x] Agency config isolated from app logic
- [x] Site audit complete (URL inventory, content inventory, redirect map) — see `docs/site-audit/`
- [x] Draft MVP sitemap + redirect map (cross-checked against the completed audit)
- [x] About Us nav link fixed live (now `/local-insurance-agent-knoxville-tn`); footer still points to the old dead URL — redirect both to `/about` (see `docs/site-audit/redirect-map.md`)
- [x] Fate of orphaned draft pages decided — `/new-page-1` (contractor stub) reinforces the Contractors MVP page; `/new-page-2` (restaurant) becomes a Phase 3 content-engine worked example, not a landing page
- [x] SMS/email nurture provider decided — **GoTo Connect (SMS, existing service) + Resend (email)**, updated 2026-08-21 from the original Twilio assumption once the owner confirmed Elite already runs GoTo (see `docs/architecture.md`). Both built and live for internal/customer notifications (see below).
- [x] GoTo SMS integration built — `src/lib/integrations/goto/client.ts` (real Messaging V2 API client, Personal Access Token exchange) + `src/lib/notifications/leadNotify.ts` (internal staff SMS on new lead/contact/claim, wired into all 3 form API routes). No-ops cleanly without credentials, verified. Owner has generated a PAT; needs to set `GOTO_CLIENT_ID`/`GOTO_CLIENT_SECRET`/`GOTO_PERSONAL_ACCESS_TOKEN`/`GOTO_OWNER_PHONE_NUMBER`/`GOTO_NOTIFY_PHONE_NUMBER` in `.env.local` to go live — steps in `.env.example`.
- [x] Resend confirmation emails built — **live and verified 2026-08-22**, not just built. `src/lib/integrations/resend/client.ts` + `src/lib/notifications/emailNotify.ts` + `src/components/emails/RequestConfirmationEmail.tsx` send a customer-facing confirmation on quote/contact/claim submission, wired into all 3 form API routes alongside the GoTo SMS staff notifications. No-ops cleanly without credentials. Real end-to-end test: standalone send returned a live Resend message ID, and a real `/contact` submission in the browser sent successfully.
- [x] Resend domain verified — **`eliteinsurancegroup.org` verified in Resend 2026-09-08** (Squarespace-managed DNS, SPF/DKIM added there). `RESEND_FROM_EMAIL` set to `Elite Insurance Group <noreply@eliteinsurancegroup.org>` in `.env.local`, confirmed with a real send through the verified domain (not the sandbox sender). **Still needs the same var added to Vercel's production environment** — until then, deployed confirmation emails keep using the sandbox sender regardless of this local config.
- [x] Build homepage per "Homepage direction" in handoff doc — hero + AI-quote CTA, line selector (12 pages), carrier proof point (text, pending logo rights), collector-car authority, team credibility, reviews link-out. Design system (navy/gold, Inter/Fraunces) built from scratch in `src/app/globals.css`.
- [x] Core scaffolding pages built and wired to real content: `/about` (real mission/community copy + team), `/reviews` (Trusted Choice link-out only, no fabricated testimonials), `/contact`, `/claims`, `/quote` (family picker) + `/quote/[family]` (6 quote forms matching the old site's field sets — see `docs/site-audit/content-inventory.md`).
- [x] Contact/Claims/Quote forms submit to local API routes (`/api/contact`, `/api/claims`, `/api/quote`), validated server-side with zod. Only quote submissions map into the canonical `Lead` schema (`src/lib/leads/mappers.ts`) — contact and claims get their own simple schemas/stores since neither is genuinely new-business sales intent. Persistence is in-memory only this pass (explicitly commented as non-durable) — durable storage is separate from this item.
- [x] Build the 12 MVP line/vertical landing pages, **commercial-first per confirmed priority lines** (see `docs/sitemap-mvp.md`): Collector Car (flagship, reuses `CollectorCarAuthority`) → Business hub → General Liability → Workers Comp → Commercial Auto → Commercial Property → Builders Risk → Commercial Umbrella → Contractors → Short-Term Rental → Group Life → Personal Auto/Home. Built via a shared `LineLandingPage` template (`src/components/marketing/LineLandingPage.tsx`) with real audited copy for the 4 lines that had it and original, factual (no fabricated rates/carrier claims) educational copy for the 8 commercial lines that didn't. Each page's quote CTA links directly to its matching `/quote/[family]` form via `resolveQuoteFormFamily()`. `LineSelector` now links with `next/link` instead of the placeholder `<a>` tags.
- [x] Durable persistence for leads/claims/contact messages — **live and verified 2026-08-21**, not just built. Postgres via Supabase + Drizzle ORM (`src/lib/db/`). Real end-to-end test: submitted actual forms in the browser and confirmed the rows in Supabase directly (`leads`, `claims`, `contact_messages` tables all populated with correct data). Falls back to the original in-memory arrays automatically if `DATABASE_URL` isn't set. See `docs/architecture.md` ("Durable persistence") for the connection-string gotcha this surfaced (Supabase's literal "direct connection" is IPv6-only and fails from IPv4 networks — use the session pooler instead for migrations).
- [x] Migrate 6 existing blog posts to `/blog` — **live and verified 2026-08-22**, full body content pulled from the live Squarespace posts (not reconstructed) and pushed to Sanity as `status: "published"`. 3 are by ex-employee Tyler Vaught, migrated as-is per the earlier decision not to route future work to him.
- [x] Sanity CMS wired up — `src/lib/sanity/` (client, GROQ queries, Portable Text helper) + `/blog`, `/blog/[slug]`, `scripts/seed-sanity-posts.mjs`. Fallback-safe (renders 0 posts without credentials), verified.
- [x] Sanity Studio embedded at `/studio` — real editing UI (`sanity.config.ts`, `src/sanity/schemaTypes/`) for Chaz to review and publish the draft posts, instead of editing raw documents via script. Required moving the marketing site into a `(site)` route group so `/studio` isn't wrapped in the site's Header/Footer — see `docs/architecture.md` ("Blog / Sanity CMS"). One manual step left for the owner: register the CORS origin for local + production, per the same doc section.
- [x] 6 new high-ROI posts drafted covering priority lines — general liability, commercial auto, boat (East TN lakes), group life, workers' comp (TN-specific), builders risk. Reviewed and published via `/studio` by the agency owner, **2026-08-22** — all 6 now live on `/blog` alongside the 6 migrated posts (12 total).
- [ ] Publish the restaurant-insurance content-engine worked example at `/blog` — per the agency owner (2026-08-22), this stays a **structural reference** for future posts rather than being published itself; the 6 new drafts above already apply its section-based pattern to real priority lines.
- [ ] **Scripted Lead Warmer** ("Quick Quote Chat") — supersedes the earlier "AI Lead Warmer" plan; decided **2026-09-03** to use a scripted decision-tree questionnaire instead of a real LLM (Elite's quote workflow is already fully standardized), plus live associate takeover. Full plan history in the session's plan file. 6 flows total, matching the 6 `quoteFormFamilies` slugs exactly (collector-vehicle, auto, home, recreational, life, business).
  - [x] Pilot flow (**auto**) built and **verified end-to-end 2026-09-03**: `src/lib/scripted-chat/` (engine + flow config), new `conversations`/`associates` Postgres tables (migration applied), `/api/scripted-chat/start` + `/answer` routes, customer chat UI at `/quote/[family]/chat` with a "Try the Quick Quote Chat instead" CTA on the matching static form. A completed chat produces the exact same validated `Lead` the static form does (reuses `quoteFormToLead()` unchanged) and fires the same `notifyNewLead`-equivalent SMS/Resend email/EZLynx pipeline. Real browser test confirmed a completed conversation lands in Postgres as `completed-unclaimed` with a linked `leadId`, and the richer chat-specific SMS notification (`notifyScriptedChatLead`) fires on the real GoTo line.
  - [x] **Script wording (`src/lib/scripted-chat/flows/auto.ts`) signed off by Chaz 2026-09-23** — approved for real customer traffic. Applies only to the auto flow; each of the remaining 5 flows is still gated on its own sign-off once built.
  - [x] Fixed two real bugs reported from live use, plus a script wording change, **verified end-to-end 2026-09-09**: (1) the answer input text was invisible (white-on-white — see the separate chat-input-color fix above) — unrelated CSS issue, already fixed; (2) the page itself scrolled on every "Send" (`scrollIntoView()` scrolls every ancestor container including the whole page — now scrolls only the transcript panel's own `scrollTop`); (3) the answer box lost focus after every send (`autoFocus` only fires on an element's first mount, and consecutive text-type questions reuse the same `<input>`, so it never re-fired after the first question — now focused explicitly via a `useEffect` keyed on the active step). Also combined the separate first-name/last-name questions into one ("what's your first and last name?"), split server-side — added a small, generic `spreadFields` capability to the engine (`src/lib/scripted-chat/types.ts`/`engine.ts`) for any single question that needs to fill more than one target-schema field at once, not just a one-off hack for this flow.
  - [ ] **Two feature ideas raised, not yet decided**: asking the customer's target budget (so an agent can match a policy to it), and asking about their current insurance coverage. The second one already has a home in the schema — `Lead.renewalUrgency` (`currentCarrier`/`renewalDate`/`hasActivePolicy`) was defined from the start but the Auto flow has never actually asked for it. Budget has no schema field yet.
  - [x] Staff invite/onboarding built and **verified end-to-end 2026-09-08**: `/staff/join` (code + email → `verifyOtp()` → set password), `/api/staff/complete-setup`, `scripts/seed-associates.mjs`. **Had to abandon the link-based invite flow entirely** — Supabase's `/auth/v1/verify` link consumes the one-time invite on a bare GET, which a corporate email security scanner (Microsoft Defender Safe Links, confirmed against Chaz's real inbox) fetches automatically before the recipient ever opens the email, silently killing every link-based invite. Confirmed technically (not just empirically) that the link's `token_hash` and the separate numeric `{{ .Token }}` code share the same underlying secret, so a scanner-consumed link burns the code too — a code shown *alongside* a clickable Supabase verify link would not have helped. Fix: the **Invite user email template in the Supabase dashboard must not reference `{{ .ConfirmationURL }}` at all** — only the numeric code (as text) and a plain link to this app's own `/staff/join` page, which is inert (visiting it triggers nothing) so it's safe for a scanner to prefetch.
  - [x] Staff login + dashboard (live queue + "needs follow-up") built and **verified end-to-end 2026-09-08**: `/staff/login`, `/staff/dashboard`, `src/proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts` — must live at `src/proxy.ts`, same level as `src/app`, not the repo root; confirmed by reading `node_modules/next/dist/docs` before writing it, after first placing it at the root and shipping a real bug — the login-page redirect-when-already-authenticated never ran because the dashboard page's own fallback check silently masked it), `/api/staff/claim`. Live browser test with a throwaway associate: route protection confirmed both directions, dashboard correctly showed real existing live/follow-up conversations, claim + atomic double-claim rejection confirmed via two separate sessions, sign-out confirmed.
  - [x] Chaz tested the dashboard live in production 2026-09-08 — confirmed login and claiming work. Found from watching his real usage: Live Queue cards showed no customer detail at all (just family + status), and there was no way to un-claim or close out a claimed conversation, so his test claims had nowhere to go. Both fixed and verified end-to-end: Live Queue cards now render whatever's in `collectedFields` live (label/value, humanized field names), and claimed-by-you cards get **Release** (back to the queue, unclaimed) and **Mark Complete** (`completed-claimed` — drops out of both Live Queue and Needs Follow-up, same as a resolved inbox item, no Lead auto-created since the associate already has full context from handling it directly). New `/api/staff/release` + `/api/staff/complete`, sharing a `verifyStaffRequest()` helper extracted from the now-3x-repeated bearer-token check (`/api/staff/claim`, `/api/staff/complete-setup` also refactored onto it).
  - [x] Cleared test debris from the `conversations`/`leads` tables that had accumulated from this session's QA (fake in-progress conversations, two fake completed leads) — kept Chaz's own genuine self-test lead intact.
  - [ ] **Live takeover (the script pausing, an associate typing directly into a live customer conversation)** — in progress as a 3-PR sequence (Supabase Realtime). `scripts/seed-associates.mjs` is ready to invite the remaining 7 confirmed associates once this ships.
    - [x] **PR 1/3, verified end-to-end 2026-09-23**: new `conversation_messages` table (one row per message, RLS enabled with zero policies — service-role/Broadcast only, never a client SELECT policy, since this holds customer PII and customers hold no auth session) replaces storing the transcript as a single jsonb blob (`conversations.data.messages`), which was a full-column overwrite with no atomic append — a real lost-update race once an associate can also be writing live. `start`/`answer` routes refactored onto `appendMessage()`/`listMessages()` (`src/lib/conversations/messages.ts`); no visible or API-contract change. Verified by running the real, now-signed-off Auto flow end-to-end in the browser: identical UI throughout, all 28 messages landed in `conversation_messages` in the correct order, `collectedFields`/lead output byte-for-byte the same shape as before, `conversations.data.messages` now `[]` on new rows. Test data cleaned up after (cascade-delete on `conversation_messages` confirmed working).
    - [ ] PR 2/3: dashboard live-refresh via `postgres_changes` on `conversations` (RLS + Realtime publication).
    - [ ] PR 3/3: live two-way chat via Realtime Broadcast (per-conversation channel, not `postgres_changes` — a public SELECT policy on message content would let anyone holding the public anon key read every customer's transcript, not just the one they're viewing), new `/api/scripted-chat/message` + `/api/staff/message` routes, `ChatWidget` live-mode switch, dashboard live-chat panel. Confirmed decision: releasing a claimed conversation ends the customer's live chat with a hand-off message rather than resuming the scripted questionnaire.
  - [ ] Also found while debugging the above: Supabase's default built-in mailer caps at **2 auth emails/hour project-wide** — fine for a handful of staff invites, not for real volume. Custom SMTP (Auth → SMTP Settings) raises this to 30/hour, but needs a Resend (or other) sending domain actually verified first — same domain-verification gap already blocking real customer-facing Resend delivery (see the Resend backlog item below).
  - [ ] Remaining 5 flows (collector-vehicle, home, recreational, life, business) not yet built — same pattern as auto, each gated on its own Chaz sign-off.
  - [ ] TN-only for now (matches the site's existing state-disclaimer gap, `src/lib/compliance/states.ts`).
- [x] EZLynx adapter scaffolded — `src/lib/integrations/ezlynx/adapter.ts`, wired into `/api/quote`. **Not connected**: EZLynx's API is partner/enterprise-gated and Elite only has a portal login today, not API credentials (see `docs/open-questions.md` for exactly what to request from EZLynx/Applied Systems). Honestly reports `{status: "not-connected"}` rather than faking a push against guessed endpoints.
- [ ] EZLynx live connection — blocked on API credentials (see open-questions.md); once granted, implement the real request logic in `pushLeadToEZLynx()`
- [x] ~~Resend account setup + domain verification (SPF/DKIM) for email nurture~~ — done, see above
- [x] GA4 analytics wired up (Measurement ID `NEXT_PUBLIC_GA_MEASUREMENT_ID`, see `.env.example`) — `page_view` automatic, `landing_page_view`/`lead_created`/`contact_submitted`/`claim_submitted` implemented and verified firing with real data (see `docs/event-tracking-schema.md`). `gclid` captured into `Lead.source.gclid` ahead of the agency owner's incoming Google Ads account access. Full source-to-lead attribution still needs durable persistence + EZLynx sync (see items above/below) — GA4 covers the marketing-visibility side, not the CRM side.
- [ ] AI-assisted content drafting/scheduling with human approval workflow — approver confirmed: Chaz Goodin (chazgoodin@gmail.com)
- [x] Redirects implemented in `next.config.ts` (20 rules, verified with `next start` — all return 308 Permanent Redirect, the modern equivalent of 301) covering every URL in the redirect map, including `/privacypolicy` → `/privacy` and `/termsandconditions` → `/sms-terms`, both new pages built with the real text pulled from the live site (`src/app/(site)/privacy`, `src/app/(site)/sms-terms`), linked from the footer. **Still pending legal/compliance review** before the DNS cutover, per the redirect map's own note. **Not deployed via DNS yet** — per the map's own rule, this needs to go live at the same time as the cutover from Squarespace, not before.
- [ ] Carrier logo/usage rights (still open) — once confirmed, swap `CarrierStrip.tsx`'s text wordmarks for real logo assets

## Phase 2+ (after MVP proves value)

- [ ] Policy/document intake (secure upload, extraction, structured agent brief — no autonomous coverage decisions)
- [ ] Renewal/cross-sell detection
- [ ] Agent copilot / internal search
- [ ] Paid acquisition: Google Search, Meta/retargeting, commercial/collector campaigns, A/B tests
- [ ] Expand landing pages beyond MVP set: boat/motorcycle/RV (recreational, demoted from MVP per 2026-08-21 priority-lines data but still valuable collector-car cross-sell), personal life insurance (distinct from Group Life, which is in MVP), cyber, additional commercial verticals (restaurants — beyond the Phase 3 blog example — transportation, medical/professional offices, retail/service, automotive)
- [ ] Expand local content beyond Knoxville/East TN into other licensed states, with genuinely differentiated (non-programmatic) content per state
- [ ] Full policy/revenue attribution + CRO + lead-score refinement + campaign feedback loops

## Later / productization

- [ ] Extract reusable primitives (landing-page framework, lead warmer, qualification flows, scoring, CRM connectors, content engine, policy intake, renewal/cross-sell, analytics, copilots) into a platform other agencies could run
- [ ] Multi-tenant agency config pattern (generalize `src/lib/config/agency.ts`)

## Explicitly not building (guardrail)

- Autonomous binding of coverage
- Definitive/licensed coverage determination workflows
