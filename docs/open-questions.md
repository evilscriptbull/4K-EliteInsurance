# Open Questions

Living doc. Update as answers come in — don't let this go stale once a question is resolved, move it to "Answered" with the date.

## Answered

- **What AMS/CRM does Elite use?** → **EZLynx.** Confirmed by agency owner, 2026-08-21. Drives `src/lib/config/agency.ts` (`crm.system`) and the (future) EZLynx-specific lead adapter.
- **Exact geographic service area?** → **TN (primary/home state) plus KY, NC, SC, VA, WV, FL, GA, AL, OH, IN, TX, AZ, MT** — 14 states total. Confirmed by agency owner, 2026-08-21. This is materially broader than the current site's Knoxville/East-TN-only framing; encoded in `src/lib/config/agency.ts` (`licensedStates`) and `src/lib/compliance/states.ts`. Content/SEO strategy for launch still prioritizes TN (see `docs/content-calendar-90day.md`), but the compliance layer and long-term paid/SEO scope now account for all 14.
- **What email/SMS provider?** → **GoTo Connect (SMS) + Resend (email)**, updated 2026-08-21. Original working decision was Twilio + Resend, made on the assumption no SMS platform existed — Elite already runs **GoTo** for phone service, which has a real Messaging V2 SMS API, so that replaces Twilio (no new account needed). Full rationale in `docs/architecture.md` ("SMS/email nurture provider decision"). GoTo SMS is built and wired into internal lead/contact/claim notifications (`src/lib/integrations/goto/client.ts`, `src/lib/notifications/leadNotify.ts`) — one-time OAuth setup still needed (owner confirmed they can get credentials easily; steps in `.env.example`). Resend still needs an account + domain verification before Phase 2 nurture ships.
- **Who approves compliance/marketing language before it goes live?** → **Chaz Goodin, chazgoodin@gmail.com.** Confirmed by agency owner, 2026-08-21. Encoded in `src/lib/config/agency.ts` (`agency.complianceApprover`). Route AI-drafted content and lead-warmer copy through this contact per `guardrails.requireHumanReviewForHigherRiskRecommendations`.
- **Which carrier appointments are relevant?** → **Erie Insurance, The Hartford, Builders Mutual, Travelers, Encova.** Confirmed by agency owner, 2026-08-21. Encoded in `src/lib/config/agency.ts` (`carriers`) — safe to name as a multi-carrier proof point per the "Homepage direction" section of the handoff doc, but naming a carrier is not the same as having rights to their logo (see still-open item below), and guardrails against fabricating rates/appetite/underwriting rules still apply even to real, correctly-named carriers.
- **Which lines have the best close rate/commission potential?** → **Workers Comp, General Liability, Builders Risk, Commercial Property, Commercial Auto, Commercial Umbrella, Group Life** — plus strong performance on Short-Term Rental/Vacation Home and New Construction/Renovation. Confirmed by agency owner, 2026-08-21. This is a **major correction** to the original handoff doc's landing-page ordering, which was personal-lines-heavy: Elite is effectively a commercial-first shop behind its collector-car flagship branding. Encoded in `src/lib/config/agency.ts` (`priorityLines`); reorders `docs/sitemap-mvp.md`.
- **Who is "Tyler Vaught"?** → Ex-employee, per agency owner, 2026-08-21. Credited author on 3 of the 6 existing blog posts. Migrate the posts; don't route future content assignments to him, and don't list him on the new About page.
- **What should happen to `/new-page-1` and `/new-page-2`?** → Per agency owner, 2026-08-21: the restaurant-insurance copy on `/new-page-2` should be used **as a worked example of a directed blog post** for the content engine (Phase 3), not built out as its own MVP landing page — restaurants aren't in the confirmed priority-lines list. `/new-page-1` (empty "Contractor Insurance" stub) still supports contractors staying an MVP vertical, since it bundles several of the newly-confirmed priority lines (GL, Workers Comp, Commercial Auto).
- **Does anyone have last-known content for the About Us page?** → Moot — the live nav link was fixed. It now points to `/local-insurance-agent-knoxville-tn` (200, real content pulled 2026-08-21; see `docs/site-audit/content-inventory.md`). Note: the **footer** "About Us" link on the live site still points to the old broken `/insurance-agent-knoxville-tn` — a live-site bug, independent of this rebuild.
- **Does the live site have any CRM/rater integration?** → **Confirmed no** — by both the Phase 0 crawl (no rater/AMS widget found anywhere) and the agency owner directly, 2026-08-21. The EZLynx lead-push adapter in the backlog is genuinely new work, not a migration.

## Still open — blocking

- **EZLynx API access.** Owner has an EZLynx portal login, not API credentials — EZLynx's API is partner/enterprise-gated (OAuth2), not self-serve (confirmed via EZLynx's published API solutions page). `src/lib/integrations/ezlynx/adapter.ts` is scaffolded and wired into the quote flow but honestly reports `{status: "not-connected"}` until real credentials exist — building request logic against guessed endpoints would fabricate something that looks connected but isn't. **What to request from EZLynx/Applied Systems:** API partner/developer access (OAuth2 client credentials), specifically for creating Applicants/Prospects and Opportunities from lead data — ask your EZLynx rep or Applied Systems support for "EZLynx API access for a third-party lead-intake integration" and request the current API reference docs once access is granted.

## Still open — non-blocking

- Existing online scheduling system, if any?
- Can quote/policy outcomes be programmatically returned from EZLynx for attribution? (Needed for the back half of `docs/event-tracking-schema.md` — `agent_contact_logged` onward. Same EZLynx API access blocker as above.)
- What access exists to analytics/Search Console/Google Business Profile/ad accounts? (Needed before Phase 0 baseline metrics can actually be pulled, and before the redirect map can be validated against real indexed URLs.)
- What permissions exist for carrier logos/images/content use on the new site? (Carrier names themselves are now confirmed — see Answered — but logo/trademark usage rights are a separate, still-open question.)
- What should happen to the `eliteinsurancegroup.org` alias domain post-launch? Currently just mirrors the primary Squarespace site; needs a DNS/redirect decision, not a content decision.
